{{- if .Values.config }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "proxmox-mcp.fullname" . }}
  labels:
    {{- include "proxmox-mcp.labels" . | nindent 4 }}
data:
  config.yaml: {{ toYaml .Values.config | b64enc | quote }}
{{- end }}
